\documentclass[sigconf,nonacm]{acmart}

\settopmatter{printacmref=false}
\renewcommand\footnotetextcopyrightpermission[1]{}
\pagestyle{plain}

\usepackage{booktabs}
\usepackage{graphicx}
% Figures live in the repo's outputs/figures (relative to this file). Multiple
% candidates so it resolves whether the compile root is this dir or the repo root.
\graphicspath{{../../outputs/figures/}{outputs/figures/}{figures/}}

% Keep each section's floats within that section, so a table or figure cannot drift
% above an earlier section heading (e.g. Table 1 / Figure 1 rising above Data).
\usepackage[section]{placeins}
% Enables [H] placement: drop a float exactly where it is written, no floating.
\usepackage{float}

% Year-over-year change tags, plain (no color): decrease, increase, no change.
\newcommand{\yd}[1]{\,{\small(#1\%)}}
\newcommand{\yu}[1]{\,{\small(+#1\%)}}
\newcommand{\yz}{\,{\small(0\%)}}

% command for wip sections
\newcommand{\wip}[1]{\textcolor{red}{#1}}

\title{Open, Reproducible Consumption-Based Carbon Intensity Forecasting with Explicit Cross-Border Flow Modeling}

\author{Nicol\'as Higuera Wilches}
\email{nicolaswilches@student.ie.edu}
\affiliation{
  \institution{IE University, School of Science and Technology}
  \city{Madrid, Spain}
}


\author{Bissan Ghaddar}
\email{bghaddar@faculty.ie.edu}
\affiliation{
  \institution{IE University, School of Science and Technology}
  \city{Madrid, Spain}
}

\begin{document}

\begin{abstract}
We present an open, reproducible deep learning framework for short-horizon (96
hour) consumption-based carbon intensity forecasting that explicitly models
cross-border electricity flows, evaluated across five structurally diverse energy grid
zones: US-MIDA-PJM (US Mid-Atlantic grid), US-NY-NYIS (New York grid), Finland, Belgium, and Singapore. The framework
reproduces the closed CarbonCast methodology as a production-based baseline,
extends it to a consumption-based target with interconnector flow and partner
carbon intensity inputs, and compares both against a single-tier model that
forecasts carbon intensity directly. We contribute a per-region accuracy atlas, a
structural comparison of the two-tier design, and a characterization of where
forecasts succeed and fail, with a feature comparison of the cross-border inputs
underway. 

\wip{On a held-out May 2026 test month, consumption-based accuracy ranges from under 1\% MAPE in near-islanded Singapore to roughly 6\% in the large US markets, with Belgium the hard case at a distribution shift the model does not anticipate. Our central finding is that the two-tier structure does not earn its complexity: a single-tier network that forecasts carbon intensity directly matches or beats the two-tier
model in every zone, because imperfect first-tier generation forecasts inject more
noise than signal. On PJM the production-based two-tier model reproduces the
published CarbonCast result (6.0 versus 5.3 MAPE), validating the reproduction.}
\end{abstract}

\maketitle

\section{Introduction and Problem}
In recent years, global electricity demand has risen sharply, growing by more than
4\% in 2024 and projected to keep climbing through the rest of the decade (IEA).
Data centers are among the fastest-growing sources of this load: their electricity
use is projected to more than double, from roughly 460 TWh in 2022 to over
1{,}000 TWh by 2026 (IEA). Meeting that demand is a challenge, but it also creates
an opening. Because a computing workload can run in different places and at
different times, and the emissions it causes depend on when and where it runs,
operators can move data center work toward cleaner grids and cleaner hours and cut
its carbon footprint. Acting on this requires an accurate, location-specific
measure of how carbon-intense a region's electricity is.
% TODO: verify exact IEA figures and add bib entries (2024 electricity demand growth; data-centre TWh projection).

Most prior work predicts a region's carbon intensity from its own energy
production. That production-based view is incomplete, because a region's real
footprint also depends on the electricity it imports from its neighbors and on how
that imported power was generated. The signal a carbon-aware scheduler actually
needs is \emph{consumption-based} carbon intensity, which accounts for the carbon
embedded in imported electricity rather than only the \emph{production-based}
intensity of locally generated power that most academic forecasters target.

This project extends previous carbon intensity forecasting frameworks to the
consumption-based target by modeling the net energy exchange between five zones and
their interconnector partners, producing a short-horizon (96 hour) forecast that
can inform systems scheduling data center workloads by a region's carbon intensity.
The contribution is not introducing consumption-based forecasting as a target,
since operational systems already exist, but opening a closed methodology, measuring
the role of energy interconnector flows, characterizing where forecasts succeed and
fail across heterogeneous grids, and providing a reproducible benchmark.

\section{Related Work}
\textbf{CarbonCast} (Maji, Shenoy, Sitaraman, ACM BuildSys 2022) is the closest
methodological reference. It is a two-tier hierarchical model: Tier~1 is a set of
per-source feedforward artificial neural networks (ANNs) that forecast generation from history, calendar features, and weather; Tier~2 is a convolutional-recurrent network that consumes
the Tier~1 forecasts together with historical carbon intensity and weather to
forecast carbon intensity. It is production-based by design, explicitly ignoring
imports and exports, and is evaluated on six regions, one of them PJM, which this
study also covers. Because it is open source, we adopt CarbonCast as the two-tier
\emph{production-based} baseline that our two-tier \emph{consumption-based} model
extends.

\textbf{EnsembleCI} (Yan et al., 2025) is an adaptive ensemble that improves on
CarbonCast for production-based carbon intensity across eleven grids including PJM,
but does not use interconnector flows. It is the strongest production-based prior
art.

\textbf{Electricity Maps operational forecast} (productionized January 2025)
provides flow-traced, consumption-based carbon intensity forecasts across more
than one hundred zones and is used by Google's carbon-intelligent computing
platform. It is a closed model, and the operational forecast our consumption-based
work is benchmarked against.

\textbf{CarbonFlex} (Hanafy et al., 2025) and the \textbf{Phoenix field
demonstration} (Colangelo et al., 2025) are not forecasting papers but
downstream consumers of carbon intensity forecasts, cited to motivate the
carbon-aware computing application rather than as baselines.

\textbf{The gap.} None of these offers open, consumption-based forecasting with
explicit cross-border flow modeling for comparable regions. EnsembleCI is the
strongest prior work but stays production-based, while the Electricity Maps forecast
is consumption-based but closed. This work fills the empty corner: an open,
reproducible model that targets consumption-based carbon intensity and models
cross-border flows explicitly.
% TODO: add formal citations / .bib entries. Decide whether to reproduce EnsembleCI on PJM or only cite.

\section{Data}
We use Electricity Maps (academic license) for hourly carbon intensity, power
breakdown, and electricity flows, and Open-Meteo (ERA5 reanalysis for history,
GFS for forecasts) for weather, over January 2021 through May 2026 across the five
zones. Carbon intensity is computed with CarbonCast Table~1 lifecycle emission
factors. For each zone we also collect the carbon intensity history of its
interconnector partners (twenty external zones), used as a consumption-based
feature.
The five zones were chosen for structural diversity: a large multi-state US market
(PJM), a smaller US market with heavy hydro imports (NYIS), a Nordic grid
undergoing a nuclear-driven decarbonization (Finland), a small import-heavy
European grid (Belgium), and a near-islanded gas-dominated grid (Singapore).

\section{Exploratory Data Analysis}
This section reports our initial evaluation of the extracted data across all five
regions.

\subsection{Carbon intensity evolution per region}

Three distinct trajectories. Finland falls sharply (161 to roughly 70 to 84), a
regime shift discussed below. Singapore is flat and high (about 490), a
near-islanded gas grid. New York rises (260 to 308), the only zone trending
upward. PJM drifts mildly down then stabilizes near 400; Belgium fluctuates
without a clear trend (150 to 195).

\begin{table}[H]
\caption{Annual mean consumption-based carbon intensity (gCO\textsubscript{2}eq/kWh),
with year-over-year change in parentheses.}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccccc}
\toprule
Year & Finland & Belgium & Singapore & PJM & NYIS \\
\midrule
2021 & 161 & 175 & 492 & 433 & 260 \\
2022 & 127\yd{-21} & 195\yu{11} & 492\yz & 429\yd{-1} & 284\yu{9} \\
2023 & 80\yd{-37}  & 173\yd{-11} & 488\yd{-1} & 397\yd{-7} & 282\yd{-1} \\
2024 & 77\yd{-4}   & 151\yd{-13} & 485\yd{-1} & 397\yz & 297\yu{5} \\
2025 & 66\yd{-14}  & 179\yu{19} & 484\yz & 412\yu{4} & 309\yu{4} \\
2026 & 84\yu{27}   & 188\yu{5} & 481\yd{-1} & 401\yd{-3} & 308\yz \\
\bottomrule
\end{tabular}}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\columnwidth]{eda_ci_evolution.pdf}
\caption{Monthly-average consumption-based carbon intensity per region, 2021 to
2026.}
\label{fig:ci-evolution}
\end{figure}

\subsection{Energy mix evolution per region}
Per-source generation share, 2021 to 2025 (Figure~\ref{fig:mix-evolution}):
\begin{itemize}
\item \textbf{Belgium:} nuclear falls sharply, 52\% to 33\%, as the phase-out
proceeds, while solar nearly triples (5\% to 15\%) and wind grows (12\% to 18\%);
gas holds near 20\%. This is the largest structural change in the set and explains
Belgium's rising, more volatile intensity.
\item \textbf{Finland:} wind more than doubles (12\% to 27\%) and nuclear edges up
(36\% to 39\%), while coal collapses (11\% to 2\%) and gas all but disappears (7\%
to 1\%); hydro eases from 23\% to 15\%. The fossil retreat explains the carbon
intensity halving.
\item \textbf{Singapore:} gas dominates throughout (97\% to 95\%) with no nuclear,
and only solar moves, from a thin 0.7\% to 2.8\%. This explains the flat, high
intensity.
\item \textbf{US-MIDA-PJM:} a coal-to-gas shift on a stable base: gas rises 38\% to
42\% and coal falls 22\% to 17\%, with nuclear steady near 32\% and solar still
small (0.7\% to 2.8\%). The mix barely moves, matching the flat intensity.
\item \textbf{US-NY-NYIS:} gas grows (48\% to 53\%) while both low-carbon sources
recede, nuclear 25\% to 21\% and hydro 22\% to 18\%, with wind only 3\% to 5\%.
This explains the rising intensity.
\end{itemize}
% [!t]: the figure scales to ~7.25in tall, above the default \topfraction limit
% for a [t] float, so without "!" LaTeX defers it and leaves a blank gap.
\begin{figure}[!t]
\centering
\includegraphics[width=\columnwidth]{eda_mix_sankey.pdf}
\caption{Generation-mix transition from 2021 (left bar) to 2025 (right bar) per
zone. Each ribbon is one energy source and its width tracks that source's share of
generation, so a narrowing ribbon is a declining source and a widening one a
growing source. Each source connects only to itself, so no cross-flows are implied.}
\label{fig:mix-evolution}
\end{figure}

\subsection{Net energy flow (imports and exports)}
\begin{table}[h]
\caption{Mean net import as a share of consumption (\%), positive = net importer.}
\begin{tabular}{lrrrrr}
\toprule
Year & Finland & Belgium & Singapore & PJM & NYIS \\
\midrule
2021 & +33 & -9  & 0 & -5.7 & -29 \\
2023 & +17 & +3  & 0 & -4.8 & -12 \\
2026 & +17 & +30 & 0 & -5.5 & -15 \\
\bottomrule
\end{tabular}
\end{table}
\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{eda_net_import.pdf}
\caption{Monthly net import as a share of consumption per region, 2021 to 2026
(positive = net importer; dotted line marks the importer/exporter boundary).}
\label{fig:net-import}
\end{figure}
Belgium swings from net exporter ($-9\%$) to heavy net importer ($+30\%$),
tracking its nuclear phase-out. Finland stays a net importer but leans on imports
less as domestic nuclear grows (33\% to 17\%). PJM and NYIS are steady net
exporters; Singapore is islanded. Dominant interconnectors by mean absolute flow:
Belgium with France, the Netherlands, and Great Britain; Finland with Sweden and
Estonia; PJM with MISO, NYIS, and TVA; NYIS with PJM and Hydro-Quebec/Ontario;
Singapore only a small link to Peninsular Malaysia.

\subsection{Carbon intensity distributions}
\begin{table}[h]
\caption{Consumption-based carbon intensity distribution per zone (gCO\textsubscript{2}eq/kWh).}
\begin{tabular}{lrrrrr}
\toprule
Zone & mean & median & std & range & skew \\
\midrule
Singapore   & 488 & 494 & 13 & 420--502 & -1.88 \\
PJM         & 412 & 407 & 46 & 266--568 & +0.37 \\
NYIS        & 288 & 287 & 40 & 151--428 & +0.05 \\
Belgium     & 175 & 163 & 64 & 48--383  & +0.60 \\
Finland     & 101 & 86  & 54 & 29--288  & +0.93 \\
\bottomrule
\end{tabular}
\end{table}
\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{eda_distribution.pdf}
\caption{Consumption-based carbon intensity distribution per region (overlaid histograms).}
\label{fig:distribution}
\end{figure*}
This explains the percentage-error pattern in the results. Singapore is tight and
high, so small relative error is easy, while Finland and Belgium are low and
right-skewed, so the same absolute error becomes a large percentage. The
consumption-minus-production gap is region-specific in sign and size: PJM $+54$,
NYIS $+19$, Belgium $+3$, Singapore about $0$, Finland $-10$. This is the core
justification for a consumption-based target.

\subsection{Temporal patterns (local time)}
\begin{table}[h]
\caption{Daily, weekly, and seasonal structure (local time).}
\begin{tabular}{lll}
\toprule
Zone & daily peak/trough & seasonal peak/trough \\
\midrule
Belgium & 22h / 13h & Jan / May \\
Finland & nearly flat & Jan / Jul \\
Singapore & 3h / 12h (slight) & flat \\
PJM & 21h / 3h & Jul / Oct \\
NYIS & 19h / 3h & Jul / Apr \\
\bottomrule
\end{tabular}
\end{table}
\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{eda_temporal_hour_dow.pdf}
\caption{Consumption-based carbon intensity by hour of day (local) and day of week,
per region. Color is each region's own carbon-intensity percentile.}
\label{fig:temporal-hd}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{eda_temporal_month_year.pdf}
\caption{Consumption-based carbon intensity by month and year, per region. Color is
each region's own carbon-intensity percentile.}
\label{fig:temporal-my}
\end{figure*}
A clear split by climate and structure. The European zones peak in winter
(heating), with Finland strongly seasonal and almost no daily cycle, consistent
with baseload nuclear plus hydro. The US zones peak in summer (cooling), with
pronounced evening daily peaks. Singapore is flat on every axis, a tropical,
islanded, gas-dominated grid, with only a slight midday dip as its small solar
share grows. Weekday-over-weekend effects appear everywhere except Singapore.

\subsection{Feature relationships and correlations}
\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{eda_correlation.pdf}
\caption{Pearson correlation of each feature with consumption-based carbon intensity, per region.}
\label{fig:correlation}
\end{figure}
% TODO: optional per-region feature-vs-CI scatterplots to complement the heatmap.
Consistent and interpretable signal. Production intensity is the dominant
predictor everywhere (0.86 to 1.00). Partner carbon intensity is strong (0.71 to
0.81) in every connected grid and weak only in islanded Singapore (0.38),
confirming it as a genuine consumption-based feature; for Belgium specifically the
import-weighted partner intensity correlates 0.83 with the
consumption-minus-production gap over about 42{,}800 importing hours. Higher
renewable share lowers intensity everywhere, most extremely in Singapore ($-0.99$,
solar displacing gas, reinforced by shortwave radiation at $-0.75$). In the large
US markets total generation is positively correlated (0.68 to 0.73): high demand
brings fossil peakers online. In the cold European grids low temperature raises
intensity (Finland $-0.58$). The autocorrelation of carbon intensity shows daily
peaks persisting past seven days, supporting the 168 hour lookback.

\section{Methodology}
\subsection{Two-tier architecture}
Following CarbonCast, the two-tier model couples per-source Tier~1 feedforward
networks that forecast generation with a Tier~2 convolutional-recurrent network
that consumes those forecasts together with historical carbon intensity, weather,
and calendar features to emit a 96 hour carbon intensity vector. We extend Tier~1
with per-interconnector flow networks that forecast signed net flow, and we add
partner carbon intensity as a Tier~2 channel. To avoid in-sample leakage, the
Tier~1 forecasts used for Tier~2 training are generated out of fold (leave one
year out); final Tier~1 models drive inference.

\subsection{Production and consumption targets}
The production-based variant (the faithful baseline) targets the domestic
generation-mix intensity. The consumption-based variant targets the
Electricity Maps reported consumption intensity and adds the cross-border channels.

\subsection{Single-tier baseline}
To test whether the two-tier structure earns its complexity, we compare against a
single-tier model: the same Tier~2 network run standalone, with no Tier~1 forecast
channels, predicting carbon intensity directly from its own history plus weather
and calendar. Because the network is identical, the comparison isolates the
contribution of the two-tier design.

\section{Experimental Setup}
\textbf{Splits.} Training 2021 through 2025, validation January through April 2026,
Test~A May 2026, and a live Test~B (June 8 to 21, 2026) head-to-head against the
Electricity Maps operational forecast. Finland uses a shortened training start of
2023 (a per-zone window), for the regime-shift reason in Section~4.1; the other
zones use the full window. Each zone has its own model, so a per-zone window costs
nothing for the stable zones.

\textbf{Training.} RMSE loss, Adam with gradient clipping. Each model is trained on
three random seeds and the model with the best validation score is kept
(Section~\ref{sec:findings}).

\section{Iterative Findings and Modeling Decisions}
\label{sec:findings}
Our methodology was shaped by a sequence of empirical findings, recorded here
because they justify the final design.

\textbf{Initialization sensitivity.} Early five-zone runs showed large
run-to-run variation driven purely by the random initial weights: on the harder
zones some seeds converged well while others diverged, to a non-finite loss or a
poor solution. On Belgium the seed-to-seed spread in percentage error exceeded the
size of any plausible modeling improvement.

\textbf{Seeding and validation-based selection.} We fixed the random seed for
reproducibility and adopted a selection rule: train several seeds and keep the one
with the best \emph{validation} score, never the test score. This removes the seed
lottery, because a divergent seed also scores poorly on validation.

\textbf{Gradient clipping.} The non-finite-loss failures were exploding gradients;
adding gradient clipping removed them, with stable seeds unaffected.

\textbf{Belgium as an outlier.} Belgium showed both the highest error and the
largest instability. Even after stabilization its best-validation model generalized
poorly from the validation months to Test~A, the validation error far below the
test error (Table~\ref{tab:atlas}), indicating a genuine distribution shift in May
2026 rather than a training artifact. We report this as a failure mode rather than
correcting it away.

\textbf{Finland regime shift and shortened training.} Inspecting carbon intensity
over time revealed the Finnish nuclear-driven regime shift (Section~4.1). We
shortened Finland's training window to begin in 2023 and re-ran the models. The
shortened window lowered Finland's consumption-based two-tier error on Test~A
(Table~\ref{tab:structural}), confirming that the pre-2023 high-carbon regime was
actively hurting the forecast. We use the 2023-onward window for Finland throughout
the results.

\section{Results}
\begin{table}[h]
\caption{Test A (May 2026) per-zone accuracy, consumption-based two-tier
(validation-selected).}
\label{tab:atlas}
\begin{tabular}{lrrr}
\toprule
Zone & MAPE (\%) & MAE & RMSE \\
\midrule
Singapore   & 0.90  & 4.17  & 6.54  \\
US-NY-NYIS  & 7.03  & 19.22 & 25.45 \\
US-MIDA-PJM & 8.30  & 31.69 & 40.25 \\
Finland     & 24.04 & 11.43 & 14.41 \\
Belgium     & 64.42 & 74.17 & 90.23 \\
\bottomrule
\end{tabular}
\end{table}
Four of five zones forecast well. Finland's percentage looks high but its absolute
error is small (11.4 on a low-carbon grid), so the percentage is
denominator-inflated. Belgium is the genuine hard case.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{results_atlas_cons_twotier.pdf}
\caption{Per-region Test A accuracy (MAPE) for the consumption-based two-tier
model, validation-selected.}
\label{fig:atlas}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{results_forecast_vs_actual.pdf}
\caption{Day-ahead (24 hour) consumption-based carbon intensity forecast versus
actual over Test A (May 2026), per zone (consumption-based two-tier,
validation-selected). Singapore is near-exact; the US markets track the daily
cycle; Finland's forecast flattens against a late-May rise; Belgium captures the
shape but misses amplitude.}
\label{fig:fva}
\end{figure}

\subsection{Structural comparison: single-tier versus two-tier}
Table~\ref{tab:structural} compares the two-tier model against a single-tier model
(the same Tier~2 network with no Tier~1 forecast channels), for both targets, per
zone, validation-selected on Test A.

\begin{table}[t]
\caption{Structural comparison: MAPE (\%), Test A, validation-selected. Lower is
better; the better of each single/two-tier pair is bold.}
\label{tab:structural}
\begin{tabular}{lrrrr}
\toprule
 & \multicolumn{2}{c}{Production} & \multicolumn{2}{c}{Consumption} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Zone & single & two-tier & single & two-tier \\
\midrule
Singapore   & \textbf{0.68}  & 0.72  & \textbf{0.71}  & 0.90 \\
US-MIDA-PJM & 6.31  & \textbf{6.04}  & \textbf{5.93}  & 8.30 \\
US-NY-NYIS  & \textbf{6.37}  & 8.90  & \textbf{5.98}  & 7.03 \\
Finland     & \textbf{15.35} & 37.24 & \textbf{12.05} & 21.89 \\
Belgium     & 36.18 & \textbf{35.99} & \textbf{41.57} & 64.42 \\
\bottomrule
\end{tabular}
\end{table}

The two-tier structure does not earn its complexity. For the consumption target
the single-tier model wins in every zone, often by a wide margin. For the
production target it wins or ties everywhere; the two-tier model is marginally
ahead only in Belgium and PJM, both within the run-to-run noise band, while it is
much worse in Finland. This is consistent with the Tier~1 diagnostic in
Section~\ref{sec:findings}: the Tier~1 production forecasts are imperfect, and
feeding them into Tier~2 adds noise rather than signal (Table~\ref{tab:tier1}). It
does not contradict CarbonCast, whose two-tier advantage is robustness to missing
and noisy source data, benchmarked against a deterministic equation baseline
rather than a learned single-tier model.
% TODO: feature comparison (flow on/off) still pending a dedicated run.

\subsection{Error by forecast horizon}
Figure~\ref{fig:horizon} traces per-zone error across the full 1 to 96 hour
horizon. Error does not simply grow with lead time. Singapore, the two US markets,
and Finland stay low and only drift up slowly, so the day-ahead band is not
markedly better than the four-day band. Belgium is the exception: its percentage
error oscillates strongly on a 24 hour cycle, between roughly 45 and 85, because its
consumption-based intensity dips low overnight and inflates the denominator at those
hours. The oscillation is present in the absolute error too, so it is a real
daily-pattern miss and not only a denominator artifact. The 1 to 24 hour band, where
the Electricity Maps head-to-head will later sit, is among the lowest-error regions
for every zone.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{results_horizon_curve.pdf}
\caption{Per-zone forecast error by horizon (1 to 96 hours), consumption-based
two-tier, Test A. Top: MAPE. Bottom: MAE. Vertical guides mark the 24, 48, and 72
hour day boundaries.}
\label{fig:horizon}
\end{figure}

\section{Discussion and Limitations}

\subsection{Tier 1 forecast quality and the two-tier structure}
A single-tier model that ignores Tier~1 entirely matches or beats the two-tier
model. Inspecting Tier~1 directly explains why. Table~\ref{tab:tier1} reports the
normalized RMSE (RMSE over the mean) of the total-generation forecast by horizon,
and Figures~\ref{fig:tier1-horizon} and~\ref{fig:tier1-pred} visualize it.

\begin{table}[h]
\caption{Tier 1 total-generation forecast error (normalized RMSE, \%) by horizon, Test A.}
\label{tab:tier1}
\begin{tabular}{lrrrr}
\toprule
Zone & h1 & h24 & h48 & h96 \\
\midrule
Singapore   & 3.1  & 4.3  & 4.6  & 5.2 \\
US-MIDA-PJM & 5.6  & 8.6  & 10.1 & 13.3 \\
US-NY-NYIS  & 8.3  & 12.3 & 14.3 & 15.8 \\
Finland     & 11.5 & 16.3 & 18.7 & 17.5 \\
Belgium     & 24.0 & 22.4 & 23.6 & 23.6 \\
\bottomrule
\end{tabular}
\end{table}

Two patterns emerge. Belgium's Tier~1 error is high at every horizon (about 24\%,
roughly flat), because its nuclear phase-out makes the generation mix volatile and
hard to forecast; feeding production this noisy into Tier~2 is why Belgium's
two-tier model is far worse than the single-tier one, and an iterated scheme would
not help since the error is not horizon-driven. PJM, NYIS, and Finland instead
degrade with horizon (PJM 5.6\% to 13.3\%), the regime where CarbonCast's iterated
24-by-4 scheme could help, at the risk of compounding error across blocks. Because
the single-tier model uses no Tier~1 input and already wins, improving Tier~1 is
about rescuing the two-tier design rather than beating the single-tier baseline.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{tier1_horizon_error.pdf}
\caption{Tier 1 total-generation forecast error (normalized RMSE) by horizon, per region, Test A.}
\label{fig:tier1-horizon}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{tier1_pred_vs_actual_pjm.pdf}
\caption{Tier 1 day-ahead (24h) forecast versus actual gas production, US-MIDA-PJM, Test A.}
\label{fig:tier1-pred}
\end{figure*}

\subsection{Other limitations}
The Electricity Maps academic forecast horizon is 24 hours rather than the
advertised 72, so the head-to-head comparison (Test~B) is restricted to the 1 to
24 hour band.
% TODO: Belgium failure-mode analysis; single vs separate model structures.

\section{Conclusion}
We built an open, reproducible deep learning framework for short-horizon
consumption-based carbon intensity forecasting and evaluated it across five
structurally diverse grids. The framework reproduces the closed CarbonCast
methodology as a production-based baseline, with the PJM two-tier model landing at
6.0 MAPE against the published 5.3, and extends it to a consumption-based target
with explicit cross-border flow and partner carbon intensity inputs.

Two results stand out. First, the per-region atlas shows wide, structurally
explained heterogeneity: a tight high-carbon island grid (Singapore) is trivial to
forecast, the large US markets sit near 6\% MAPE, and the small import-heavy,
rapidly decarbonizing European grids are hardest, with Belgium the clear failure
case driven by a May 2026 distribution shift its training window does not cover.
Second, the structural comparison overturns the assumption behind the two-tier
design: a single-tier network that predicts carbon intensity directly matches or
beats the two-tier model in every zone, decisively so for the consumption target.
The cause is visible in the first tier, whose generation forecasts are imperfect
enough that passing them downstream adds noise rather than signal. This does not
contradict CarbonCast, whose two-tier benefit is robustness against missing source
data measured against a deterministic baseline, not against a learned single-tier
model.

The work has clear next steps: a feature comparison isolating the contribution of the
cross-border flow and partner inputs, the live Test~B head-to-head against the
Electricity Maps operational forecast over the 1 to 24 hour band, and a
Belgium-specific failure-mode analysis aimed at the distribution shift the shared
model misses.

\end{document}
